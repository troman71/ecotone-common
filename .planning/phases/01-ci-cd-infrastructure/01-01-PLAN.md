---
phase: 01-ci-cd-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Pushing a commit to main or develop triggers GitHub Actions CI"
    - "Opening a PR against main or develop triggers GitHub Actions CI"
    - "CI runs pytest across Python 3.11, 3.12, 3.13 matrix"
    - "CI runs ruff linting and format checking"
    - "CI fails if any test fails or any lint violation exists"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "pytest"
    - path: "pyproject.toml"
      provides: "pytest and ruff configuration"
      contains: "tool.ruff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pip install -e . reads project config; ruff/pytest read tool config"
      pattern: "pip install -e \\."
---

<objective>
Configure pytest and Ruff in pyproject.toml and create a GitHub Actions CI workflow that runs linting and testing on every push and PR.

Purpose: Satisfies CICD-01 (pytest on push/PR) and CICD-02 (linting enforced in CI). After this plan, every commit is automatically quality-checked.

Output: Updated pyproject.toml with [tool.pytest.ini_options] and [tool.ruff] sections; new .github/workflows/ci.yml workflow file.
</objective>

<execution_context>
@/Users/timothyroman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timothyroman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ci-cd-infrastructure/01-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest and Ruff configuration to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
    Add three new sections to the existing pyproject.toml (do NOT modify existing sections):

    1. [tool.pytest.ini_options]:
       - pythonpath = ["src"]
       - testpaths = ["tests"]
       - addopts = ["--import-mode=importlib"]

    2. [tool.ruff]:
       - target-version = "py311"
       - line-length = 100

    3. [tool.ruff.lint]:
       - select = ["E4", "E7", "E9", "F", "B"]
       - ignore = ["E501"]

    After adding config, verify locally:
    - Install ruff: pip install ruff
    - Run `ruff check src/ tests/` — fix any violations that appear (these are real bugs or style issues in existing code)
    - Run `ruff format --check src/ tests/` — if formatting violations exist, run `ruff format src/ tests/` to auto-fix
    - Run `pytest tests/` — confirm all 32 tests still pass with the new pytest config

    Important: Do NOT remove or modify any existing pyproject.toml content. Only append new tool config sections.
  </action>
  <verify>
    - `ruff check src/ tests/` exits with code 0 (no violations)
    - `ruff format --check src/ tests/` exits with code 0 (no formatting issues)
    - `pytest tests/` exits with code 0 (all 32 tests pass)
    - pyproject.toml contains [tool.pytest.ini_options], [tool.ruff], and [tool.ruff.lint] sections
  </verify>
  <done>
    pyproject.toml has complete pytest and ruff configuration. Both tools run cleanly against the existing codebase with zero violations and all tests passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create the directory .github/workflows/ and the file ci.yml with:

    1. Name: "CI"
    2. Triggers:
       - push to branches: [main, develop]
       - pull_request to branches: [main, develop]
    3. Two jobs:

    Job "lint" (runs once on ubuntu-latest, Python 3.11):
       - actions/checkout@v4
       - actions/setup-python@v5 with python-version "3.11" and cache "pip"
       - Install: python -m pip install --upgrade pip && pip install ruff
       - Run: ruff check .
       - Run: ruff format --check .

    Job "test" (matrix on ubuntu-latest):
       - strategy.fail-fast: false
       - strategy.matrix.python-version: ["3.11", "3.12", "3.13"]
       - actions/checkout@v4
       - actions/setup-python@v5 with matrix python-version and cache "pip"
       - Install: python -m pip install --upgrade pip && pip install -e . && pip install pytest
       - Run: pytest tests/

    The lint job and test jobs run in parallel (no needs: dependency between them).

    Use exact YAML syntax from the research document's "Complete GitHub Actions Workflow" example. Do not add coverage, code scanning, or any extras beyond what CICD-01 and CICD-02 require.
  </action>
  <verify>
    - File .github/workflows/ci.yml exists
    - YAML is valid (python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" or similar)
    - File contains "pytest" and "ruff" commands
    - File triggers on push and pull_request to main and develop
    - Matrix includes 3.11, 3.12, 3.13
  </verify>
  <done>
    GitHub Actions workflow file exists and is valid YAML. It defines lint and test jobs that will run on every push and PR to main/develop branches. Tests run across Python 3.11, 3.12, and 3.13.
  </done>
</task>

</tasks>

<verification>
1. `ruff check src/ tests/` — exits 0
2. `ruff format --check src/ tests/` — exits 0
3. `pytest tests/` — all 32 tests pass
4. `.github/workflows/ci.yml` exists and is valid YAML
5. pyproject.toml contains [tool.pytest.ini_options], [tool.ruff], [tool.ruff.lint]
6. No existing pyproject.toml content was removed or modified
</verification>

<success_criteria>
- pyproject.toml has pytest config (pythonpath, testpaths, import-mode) and ruff config (target py311, line-length 100, select E4/E7/E9/F/B)
- .github/workflows/ci.yml triggers on push+PR to main+develop, runs ruff lint+format check, runs pytest across 3.11/3.12/3.13 matrix
- All existing tests pass with new configuration
- No lint violations in existing codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-cd-infrastructure/01-01-SUMMARY.md`
</output>
